# Agent runtime INSIDE the sandbox. Pin these in production (release by digest).
# claude-agent-sdk: primary substrate (the wheel embeds the CLI — no global node).
claude-agent-sdk>=0.2
pydantic>=2
httpx>=0.27
