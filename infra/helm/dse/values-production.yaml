# Always use it on top of the safe pilot baseline:
#   helm upgrade --install ... -f values-pilot.yaml -f values-production.yaml \
#     -f values-<tenant>.yaml
# The same fail-closed validations apply; production relaxes no gate.
deploymentProfile: production
